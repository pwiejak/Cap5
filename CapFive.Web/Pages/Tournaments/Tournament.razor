@page "/tournament"
@page "/tournament/{Id:int}"
@inherits TournamentBase
@using CapFive.Web.Pages.Common

@if(Tournament == null && string.IsNullOrWhiteSpace(ErrorMessage))
{
    <Spinner />
} 
else if(!string.IsNullOrWhiteSpace(ErrorMessage))
{
    <DisplayError ErrorMessage="@ErrorMessage"></DisplayError>
}
else if(Tournament?.Status <= CapFive.Shared.Tournament.TournamentStatus.Created)
{
    <TournamentForm TournamentDto="Tournament" AllPlayers="AllPlayers"/>
} else
{
    <TournamentDetails Tournament="Tournament"/>
}